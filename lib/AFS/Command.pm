package AFS::Command;

use strict;
use warnings;
use English;

use AFS::Command::Version;

use AFS::Command::Base;

use AFS::Command::VOS;
use AFS::Command::BOS;
use AFS::Command::PTS;
use AFS::Command::FS;

1;
